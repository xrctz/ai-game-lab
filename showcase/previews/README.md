Card preview art (1200×750 PNG). Regenerate after editing the generator:

```bash
pip install pillow
python scripts/generate_preview_images.py
```

Hub and Showcase pages load these files from `/ai-game-lab/showcase/previews/*.png`.
