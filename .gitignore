.DS_Store
Thumbs.db
*.log

# Secrets and local env
.env
.env.*
*.env
!.env.example

# Dependencies and build artifacts
node_modules/
dist/
*.local

# Native game build output (.NET / Rust)
bin/
obj/
target/
*.so
*.dll
*.pdb

# Blender working files (not used at runtime; export glb/png instead)
*.blend
*.blend1
# Blender QA render previews (regenerate with RENDER=1)
scripts/blender/previews/

# Python caches
__pycache__/
*.pyc

# Editor / agent session files (may contain local paths or notes)
.cursor/
CLAUDE_MEMORY.md
*.pem
*.key
id_rsa*
credentials.json
