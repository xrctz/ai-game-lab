[package]
name = "notd_sim"
version = "0.1.0"
edition = "2021"
description = "Night of the Dead — Rust combat/AI simulation core (FFI for C#)"

[lib]
name = "notd_sim"
crate-type = ["cdylib"]

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
